<IfModule mod_rewrite.c>
    RewriteEngine On

    # Forward project-root requests to Laravel's public directory.
    # Prevent rewrite loop: public/* must not become public/public/*.
    RewriteCond %{REQUEST_URI} !/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
