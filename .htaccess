<IfModule mod_rewrite.c>
    RewriteEngine On

    # XAMPP / shared-hosting style: project root is web-accessible.
    # Forward every request into Laravel's public directory.
    RewriteRule ^$ public/ [L]
    RewriteRule (.*) public/$1 [L]
</IfModule>